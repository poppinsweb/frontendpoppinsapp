// function generateToken() {
//     const length = 10;
//     const characters = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz';
//     let token = '';
  
//     for (let i = 0; i < length; i++) {
//       const randomIndex = Math.floor(Math.random() * characters.length);
//       token += characters[randomIndex];
//     }
  
//     return token;
//   }

//   export default generateToken;
  
  /*esta funcion genera token de 10 digitos pero no garantiza que sean unicos mejor revisar nanoid
  En este ejemplo, la función generateToken() genera un token aleatorio de 10 dígitos utilizando una cadena de caracteres permitidos. La función utiliza un bucle for para seleccionar aleatoriamente un carácter de la cadena de caracteres y construir el token. Finalmente, el token generado se muestra en el componente tokenbox 
  Pendiente por validar funcionalidad */
  